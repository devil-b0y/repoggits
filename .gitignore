node_modules/
# one pattern for every build directory, so an ad-hoc one from a test run cannot ride along in a commit
.next*/
out/
*.tsbuildinfo
test-results/
.env*
!.env.example
.local/
backups/
# Server-side database registry and sealed passwords: never in a commit.
.data/
# Generated session reports and the screenshots they are built from. Written on demand, never part of the repo —
# note this is deliberately not *.pdf, because docs/Repoggits-Overview.pdf is a tracked document.
Repoggits-session-report.pdf
docs/Repoggits-Session-Report-*.pdf
report-shots/
