# Studio hero background

`circuit-brain-bg.webp` — user-supplied image, used as the dark-theme background behind the
"Your idea. / Its next chapter." hero on the project submission page (`.studio-hero` in
`components/platform/submission-polish.css`). Light theme is unaffected; the image and its
overlay gradients only apply under `.platform.dark`.
